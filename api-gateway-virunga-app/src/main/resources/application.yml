spring:
  application:
    name: api-gateway-virunga
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
server:
  port: 8765

# For debugging
logging:
  level:
    org.springframework.security.authentication: DEBUG
    org.springframework.security.web: DEBUG
    org.springframework.security: DEBUG